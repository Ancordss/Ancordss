despues de hacer todo eso uno se pregunta como me recordare de todos los comandos
por eso crearon docker compose

lo que haremos sera crear un nuevo archivo llamado docker-compose.yaml

para poder usar docker-compose solo debemos de tener
instalado docker-compose que es un binario este binario
ya viene con docker desktop

si no lo tienes instalado puedes correr sudo apt install docker-compose

en la seccion de servicios
declaramos los serivcios
esto permite correr varios contenedores
usando un solo archivo y automaticamente los colocara en la misma red

en este archivo solo se simplifica el uso de colocar todo el gran comando puedes comparar el archivo
docker-compose.yaml en la carpeta 12/multi-container con los comandos usados

para correr el docker-compose usaremos 

docker-compose up -d

si queremos bajar todo lo que haremos sera

docker-compose down 

